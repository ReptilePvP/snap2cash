
// This file can be expanded with more constants as needed.
// For now, it's a placeholder. Routes are defined directly in App.tsx for better co-location with Phosphor Icons.
export const APP_NAME = "Snap2Cash (NDResells)";

// Mock API delay in milliseconds
export const MOCK_API_DELAY = 1500;

export const DEFAULT_THEME = 'light';